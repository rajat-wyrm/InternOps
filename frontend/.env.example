# Base URL of the InternOps backend API (protocol + host, no trailing
# slash — e.g. https://api.example.com). See src/lib/axios.js for how
# this is normalized to end in /api/v1.
VITE_API_URL=http://localhost:5000
